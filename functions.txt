export default function App() {
      // Define a function that immediately syncs state
                  if (typeof (window as any).saveUserSettings === 'function') {
                if (typeof (window as any).saveUserSettings === 'function') {
